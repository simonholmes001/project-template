{
  "name": "{{REPO_NAME}}",
  "version": "0.1.0",
  "private": true,
  "description": "{{PROJECT_DESCRIPTION}}",
  "scripts": {
    "test": "node --test .github/scripts/*.test.mjs tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
