# Backend Function Module

This module is a marker for backend/function projects.

The current template keeps product-specific backend source code out of the shared template. Add backend runtime files in the target repository, then wire package/deploy commands into the generated infrastructure workflow.

